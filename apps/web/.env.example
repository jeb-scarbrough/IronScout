# Auth Configuration
# JWT_SECRET MUST be the same value across all apps (api, web, dealer, admin)
NEXTAUTH_URL="http://localhost:3000"
JWT_SECRET="your-shared-jwt-secret"

# OAuth Providers
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:8000"

# =============================================================================
# Premium Feature Flags (Client-side)
# =============================================================================
# Master switch for premium UI. When false:
# - Hides all premium-related UI (upgrade banners, pricing links, billing nav)
# - Hides tier badges
# - Redirects /pricing and /dashboard/billing to home/dashboard
# MUST match FEATURE_PREMIUM_ENABLED on the API server
NEXT_PUBLIC_FEATURE_PREMIUM_ENABLED="false"

# Optional granular flag (inherits from NEXT_PUBLIC_FEATURE_PREMIUM_ENABLED if not set)
# NEXT_PUBLIC_FEATURE_PAYWALL_ENABLED="true"

# =============================================================================
# Stripe (Frontend) - only needed when premium is enabled
# =============================================================================
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your-stripe-publishable-key"
