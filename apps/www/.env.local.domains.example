# IronScout Marketing Site (www) - Local Domain Configuration
#
# Merge these values into your .env.local when using local domain aliases.
# Requires DNS: *.local.ironscout.ai → 127.0.0.1

# Main web app URL (for CTA links)
NEXT_PUBLIC_APP_URL=https://app.local.ironscout.ai
