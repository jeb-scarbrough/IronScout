# IronScout Merchant Portal - Local Domain Configuration
#
# Merge these values into your .env.local when using local domain aliases.
# Requires DNS: *.local.ironscout.ai → 127.0.0.1

# API URL
NEXT_PUBLIC_API_URL=https://api.local.ironscout.ai

# NextAuth configuration
NEXTAUTH_URL=https://merchant.local.ironscout.ai

# Web app URL (for cross-app links)
NEXT_PUBLIC_WEB_URL=https://app.local.ironscout.ai
